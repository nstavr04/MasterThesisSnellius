My guide on how to use GPTCast

- I cannot run install_python_ubuntu.sh because of restricted permissions. Instead I can ran unload_moadules.sh to remove all modules and then load
Python/3.12.3 through the modules.

- Environment has been created successfully.

# activate the environment
source .venv/bin/activate  

- Under the notebooks folder there are guides on how to use the pretrained models.

See the notebook notebooks/example_gptcast_forecast.ipynb for running the models on a test batch and generating a forecast.

See the notebook notebooks/example_autoencoder_reconstruction.ipynb for a test on the VAE reconstruction.